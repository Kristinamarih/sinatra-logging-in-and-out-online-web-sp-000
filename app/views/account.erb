<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <h3>Welcome<%= Helpers.current_user(session).username %></h3>
    <h4>Your Balace:<%= Helpers.current_user(session).balance %></h4>
    <% else %>
    Please <a href="/">log in</a> to view your balance
    <% end %>
    
    <a href="/logout">Log Out</a>
  </body>
</html>